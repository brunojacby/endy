<div class="relative">
    <img src="/images/bg-visual_logo_black_large.png" alt="Bem-vindo a Endy Mesquita" class="w-full h-[80px] md:h-[400px] bg-center bg-no-repeat bg-cover" />
</div>
<section>
    {{#each categories}}
    <div class="categories-div">
        <h2>{{uppercase categoryName}}</h2>
        <div class="container-imgs">
            {{#each categoryProducts}}
            <div class="image-container">
                <img alt="{{name}}" src="{{image}}" width="100%" height="100%" />
                <a class="overlay" href="/produtos/">
                    <span>{{uppercase name}}</span>
                    <div>
                        <span>{{formatPriceBRL price}}</span>
                        <span class="btn-plus">+</span>
                    </div>
                </a>
            </div>
            {{/each}}
            <a class="todos-div" href="/categoria/">VER TODOS</a>
        </div>
    </div>
    {{/each}}
</section>
{{>about}}


<link rel="stylesheet" href="/css/index.css">